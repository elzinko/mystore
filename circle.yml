machine:
  java:
    version: oraclejdk8

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t mystore .

test:
  override:
    - docker run -d -p 8080:8080 mystore; sleep 10
    #- curl --retry 10 --retry-delay 5 -v http://localhost:8080
    #- mvn clean test
deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
